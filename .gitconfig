[alias]
        # Stage and commit all changes
        sca = !git add -A && git commit -m
[init]
        # change default branch name to main
        # git version 2.28 or higher required
        defaultBranch = main
